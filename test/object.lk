class Rect {
    'width': 0, 
    'height': 0
}

fn Rect:square(len) {
    self.width = len
    self.height = len
    rt self
}

fn Rect:printArea() {
    print("Rect area: ", self.width * self.height)
}

fn Rect:set(width, height) {
    self.width = width
    self.height = height
}