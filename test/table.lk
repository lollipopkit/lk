shy tb = {5, 'd': 4, 'c': num('2')}
print(tb[1])

shy keys, vals = kv(tb)
for i, k in keys {
    print(k, vals[i])
}

insert(tb, 2, 3)

shy fn sort_fn(e1, e2) {
    rt e1 > e2
}
sort(tb, sort_fn)

delete(tb, 1)

for key, value in tb {
    print(key, value, type(value))
}