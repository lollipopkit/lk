pri := fn (tb) {
    for key, value in tb {
        print(key, value, type(value))
    }
    print()
}

tb := {5, 'd': str(4), 'c': false}
pri(tb)

// 赋值
tb[2] = 1
pri(tb)

// 删除
tb['d'] = nil
pri(tb)

print(tb[0])