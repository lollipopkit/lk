class speed {
    'times': 100,
}

fn spedd:test(func) {
    times := 0
    t1 := os.time()
    
    while times < self.times {
        func()
        times++
    }

    t2 := os.time()
    print(fmt('time: %d', t2 - t1))
}
