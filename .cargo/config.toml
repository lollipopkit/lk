# getrandom 0.3 needs an explicit backend on wasm32-unknown-unknown (the browser
# playground crate `lk-wasm` uses randomness transitively). This is target-scoped
# so native and other wasm smoke builds are unaffected.
[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]
