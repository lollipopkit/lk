// Bench: Empty loop — measures pure loop / iteration overhead
import time;

let iters = 100000;
let t0 = time.now();
let count = 0;
for _ in 1..=iters {
    count += 1;
}
let t1 = time.now();
println("empty_loop: iters=${iters}, count=${count}, elapsed=${t1 - t0}ms");