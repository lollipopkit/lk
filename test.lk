import "test2"

shy a = {"a", "b", 'c'}
shy p,pp, ppp = pairs(a)

shy func = fn (e) {print(e)}
func(1!=2 and 3!=4)

if #a >= 0 {
    print("hello")
}

for b,c in p, pp {
    print(b,c)
}

shy code, resp = http.req('GET', 'https://api.backend.cust.team/')
print(code, resp)

shy ok, data = json.get(resp, 'data')
print(ok, data)