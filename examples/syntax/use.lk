// File use demo
// Run from project root: lk examples/syntax/use.lk

use "../general/fib";

const iters = 50000;
const n = 30;

let acc = 0;
for _ in 0..iters {
  acc = fib.iterative(n);
}

println("fib(${n}) = ${acc}, iters=${iters}");
