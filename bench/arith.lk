import os;

// Arithmetic loop benchmark
let start = os.clock();
let total = 0;
let i = 0;
while (i < 1000000) {
    let step = i + 1;
    total = total + step * (step + 1);
    i = i + 1;
}
let elapsed = os.clock() - start;
println("lk arith 1M: total={}, time={}s", total, elapsed);