import os;

// Function call overhead benchmark
fn add(a, b) {
    return a + b;
}

let start = os.clock();
let result = 0;
let i = 0;
while (i < 100000) {
    result = add(result, 1);
    i = i + 1;
}
let elapsed = os.clock() - start;
println("lk call 100k: result={}, time={}s", result, elapsed);