import os;

// Empty loop overhead benchmark - measure dispatch cost
let start = os.clock();
let i = 0;
while (i < 1000000) {
    i = i + 1;
}
let elapsed = os.clock() - start;
println("lk empty loop 1M: time={}s", elapsed);