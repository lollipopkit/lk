// Bench: Empty function call overhead
import time;

fn one() { return 1; }

let iters = 1000000;
let t0 = time.now();
let acc = 0;
for _ in 1..=iters {
    acc += one();
}
let t1 = time.now();
println("empty_func_call: iters=${iters}, acc=${acc}, elapsed=${t1 - t0}ms");
