# LK VM 重构交接进度

本文只记录当前快照、已验证事实、未完成风险和下一步执行顺序。`plan.md` 是架构契约，不写日常流水账；本文也要保持短小，避免旧 session 历史压过当前事实。

## 当前总体状态

主线已经从旧 VM 兼容迁移转为新架构收口，同时进行 LLVM true native AOT 修复。

### 架构完成度（对照 plan.md 12 项执行顺序）

| # | 项目 | 状态 |
|---|------|------|
| 1 | RuntimeVal/HeapValue/Callable/typed container | ✅ 已建立 |
| 2 | Slot-based HeapStore, GC 标记遍历 | ✅ 已建立 |
| 3 | Instr32, typed const pool, encoder/decoder | ✅ 已建立 |
| 4 | Compiler + Executor 最小可运行 | ✅ 完整端到端 |
| 5 | literal/load/move/arithmetic/branch/closure/return | ✅ 已覆盖 |
| 6 | shared stack call ABI | ✅ 已覆盖 |
| 7 | closure upvalue cell | ✅ 已覆盖 |
| 8 | per-task STW GC | ✅ 已覆盖 |
| 9 | VM handler stack, TryBegin/TryEnd/Raise | ✅ 已覆盖 |
| 10 | global/context slot, typed container fast path | ✅ 已覆盖 |
| 11 | legacy 残留清理 | 🔵 已审计，无旧 Op/BC32/quickening/Frame32 残留 |
| 12 | LLVM true native AOT | 🟡 进行中 |

### 当前已验证项

- `cargo test -p lk-core --lib` → **732 passed, 0 failed** ✅
- `cargo test -p lk-core --features llvm llvm::tests:: -- --nocapture` → **206 passed, 0 failed** ✅
- `cargo test -p lk-stdlib` → **113 passed, 0 failed** ✅
- `cargo build -p lk-cli --features llvm` → **通过** ✅
- `cargo check -p lk-core --features llvm` → **通过** ✅
- `cargo check -p lk-cli --features llvm` → **通过** ✅
- `target/debug/lk compile exe /tmp/wc_until_sort.lk --output /tmp/wc-until-sort && /tmp/wc-until-sort` → **通过** ✅
- `target/debug/lk compile exe examples/general/word_count.lk --output /tmp/lk-word-count && /tmp/lk-word-count` → **输出 `word_count: all assertions passed`** ✅
- `target/debug/lk compile exe examples/stdlib/map_demo.lk --output /tmp/lk-map-demo && /tmp/lk-map-demo` → **输出 `map_demo: all assertions passed`** ✅
- `target/debug/lk compile exe examples/general/sort_search.lk --output /tmp/lk-sort-search && /tmp/lk-sort-search` → **输出 `sort_search: all assertions passed`** ✅
- `cargo test -p lkrt` → **0 passed, 0 failed** ✅
- `examples/` sweep → **TOTAL=47 OK=40 VM_FAIL=7 COMPILE_FAIL=0 NATIVE_FAIL=0 DIFF=0**；剩余 VM_FAIL 发生在 VM/typechecker/import resolver 层，不是 native lowering 失败；当前额外 VM_FAIL 来自新增 `examples/stdlib/comprehensive.lk` 的 `Compiler32 undefined callable __lk_call_method`。
- LLVM native stdlib 方向已切到 typed `lkrt` runtime + 纯 LK stdlib source，禁止继续把完整 stdlib 方法语义散落手写到 LLVM 后端 ✅
- 最小 native executable 可链接 `lkrt`，`nm` 可见 `lkrt::link_anchor/version`，`strings` 未发现 `execute_module`、`Module32Artifact`、`Instr32 VM`、`VmContext`、`lk_core::vm`、`compile_program32`、`rt::init_runtime`、`VM runtime` 等 shell/runtime 痕迹 ✅
- 无旧 Op runtime、BC32、packed executor、quickening、旧 fused opcode 残留 ✅

## 未完成风险

### LLVM 后端（第 12 项）

当前 `examples/` 覆盖不是完成证明，且最近完整 sweep 暴露了外围与 LLVM 混合问题。LLVM stdlib 收口方向已经调整为 typed `lkrt` + 纯 LK stdlib source，不能继续在 LLVM 后端手写完整 stdlib 方法体：

1. **spec/语言 shape 覆盖审计不足** —— 需要对照 parser/type checker/compiler 支持的语法和 runtime value shape，列出 LLVM 必须 native-lower 的完整矩阵。
2. **unsupported reason 收口不足** —— examples 和 LLVM 单测之外仍可能存在会落到泛化 `unsupported Instr32/native value shape` 的组合；需要逐类压成 true native lowering 或明确 reason。
3. **stdlib native 架构迁移** —— `lkrt` link boundary 已作为第一步，后续要把纯 stdlib 方法迁到 LK source/monomorphization，host primitives 只走 registry。
4. **examples 剩余失败需分类修复** —— `import*.lk` 从 repo root 运行仍是 resolver/cwd 问题；`json_process.lk`、`string_methods.lk`、`named_args.lk` 当前 VM/typechecker/example 自身失败；`error_handling.lk` 已恢复 native executable 与 VM 一致输出。
5. **callable/container/object 显示语义仍需系统化** —— 后续若要求 callable/object 等非 scalar return 也 native-lower，需要实现与 VM display 一致的真实 native 表示。
6. **文件大小边缘** —— `facts.rs`、`blocks.rs`、`straightline_value.rs` 仍接近 1500 行，后续改动必须继续拆分。

### 已修复的 LLVM 后端问题

1. Not 指令在 scalar_facts 中接受 I64/F64/StrPtr（之前只接受 Bool/Nil）
2. native_builtin_return_kind 新增 Panic 支持
3. GetIndex 支持未知动态容器回退
4. Call 不要求所有 Builtin 参数是静态值
5. CoreCallMethod 默认返回 I64
6. 自递归 callee 自动检测 + I64 回退 hints
7. subfunction.rs 新增参数类型候选、StrPtr 参数、ToString/ConcatString text-part 保留
8. scalar_blocks.rs 在发射 @lk_fn_X 调用前预检查 subfunction 编译可行性
9. native main nil return 静默，避免 examples 末尾多输出 `nil`
10. 清理 scalar_facts 未加开关 debug stderr 输出
11. 修复 `match.lk` native 输出：assert direct call 不再回退，比较 lowering 优先使用当前 block 的本地类型，避免 stale facts 把整数/字符串寄存器误判
12. 修复静态 Test 的 untaken-path 标记：遇到多 predecessor merge 起点不再把后续可达路径跳过
13. control-flow `GetIndex` 可在 boundary 后回溯只读 heap const int list，静态 list loop 的 LLVM 回归恢复
14. control-flow template string equality 支持动态 `Text` 与静态字符串比较，覆盖 nullish/template interpolation assert
15. block lowering 对静态 I64 二元运算保留折叠值，支持多参数 `println("{} + {} = {}", ...)`
16. `container.rs` 拆出 `container/index.rs`，保持单文件低于 1500 行
17. 静态 direct-call 折叠可在同一 basic block 内恢复 heap-const list 参数，并支持静态 `list.skip(1)`；`examples/general/test_list_sum.lk` 已通过 native/VM diff
18. 自递归 hint 探测支持按参数组合尝试 I64/F64/Bool/list-like profile；`examples/general/recursive.lk` 中 `contains(List<Int>, Int) -> Bool`、`length(List<Int>) -> Int` 已通过 native/VM diff
19. Float opcode lowering 支持 I64/F64 混合 operand，通过 `sitofp` 生成 double 运算；`examples/syntax/numeric_auto_promotion.lk` 已通过 native/VM diff
20. CFG merge block 统一视为静态事实边界，避免 `match.lk` 中分支字符串事实污染合流后的比较；`examples/syntax/match.lk` 已恢复 native/VM diff
21. control-flow `Contains` 支持静态 string/list membership，包括 heap-const int list 的 `DynamicIntList` 回查；`examples/syntax/operators.lk` 已通过 native/VM diff
22. direct callee static evaluation 不再跳过无参 callee 内部构造的 const list/map，覆盖 `[10, 20].1` 和 `"b" in {"a": 1, "b": 2}`。
23. static map entry loop 支持 mixed pair list 的动态外层索引，`for (key, value) in {"a": 1, "b": 2}` 可 native lower。
24. block return 遇到静态 `Function`/`Closure`/`Builtin`/`Module` 不再误当 I64 打印，并支持 covered callable/builtin display，避免 native/VM 输出不一致。
25. `lkrt` workspace crate 与 `lk compile exe` 链接边界已建立；最终 native binary 允许链接 typed native runtime，但仍禁止 VM shell、`Module32Artifact`、`VmContext`、compiler/parser 进入。
26. 修复 branch/template string optional compare：动态 template `Text(parts)` 会物化到寄存器 text buffer，`StringPtr` move 会写回 slot，非支配分支的静态事实不再误用于 fold；新增 `llvm_backend_lowers_branch_template_string_optional_compare_without_shell` 回归。
27. 修复 direct map.get missing nil compare 与 `error_handling.lk` native 误编译：facts 只在可信静态寄存器上折叠比较；compare lowering 只把“上一条指令直接 LoadNil”当作局部 nil 操作数，非支配分支的 nil 不再污染合流后的 string/template 比较；新增 `llvm_backend_lowers_direct_missing_map_get_nil_compare_without_shell` 回归。`examples/syntax/error_handling.lk` native executable 输出 `error_handling: all assertions passed`。
28. 修复 template string compare 的动态 `StrPtr` 分类缺口；`examples/syntax/template_strings.lk` 已 native 编译运行，输出 `template_strings: all assertions passed`。
29. 动态 `Map<str, i64>` 的 `map.set(freq, word, value)` 已在 facts 和 block lowering 两侧保持 typed `DynamicMap`，新增 `llvm_backend_lowers_dynamic_string_int_map_set_call_without_shell` 回归。
30. 动态 `Map<str, i64>` 迭代新增 typed iterator/entry shape，`for pair in freq { pair[0], pair[1] }` 的缩小样例 `/tmp/wc_until_max.lk` 已 native 编译运行。
31. `StrPtr` ordered compare 已支持 `strcmp` lowering，作为 `sort_words` 后续 native 化的基础。
32. `native_direct_call_static_return_value` 不再尝试静态求值含 `Test/Jmp/CallDirect/CallNamed` 的控制流函数，避免 `sort_words` 这类函数在 static evaluator 内栈溢出。
33. `scalar_facts` 入口逻辑拆到 `core/src/llvm/scalar/facts/entry.rs`，继续遵守单文件 1500 行限制。
34. `List<str>`/`List<ptr>` native lowering 已覆盖 `sort_words` 所需路径：静态字符串列表调用点会物化为 ptr-list ABI，`@lk_fn_X_list` 子函数支持 `GetIndex`、`ListPush`、`take`、`skip`、`concat` 和 list return。
35. ptr-list 子函数中的 list-valued local 使用稳定 `listreg<reg>` storage，`Move` 对 list 做真实 copy，修复循环变量 `remaining = before.concat(after)` 回跳后仍读旧 storage 的问题。
36. `blocks.rs` 继续拆分到 `blocks/list_methods.rs`、`blocks/not.rs`、`blocks/iter.rs`、`blocks/string_methods.rs`，当前主文件处于 1500 行上限。
37. 新增 `llvm_backend_lowers_string_list_sort_ptr_list_call_without_shell` 回归，覆盖 `List<str>` sort 的 ptr-list direct call、`take` 和 `concat` lowering。
38. 静态 `map.set` 不再阻断静态 map 路径，`examples/stdlib/map_demo.lk` 已 true native executable 通过；新增 `llvm_backend_lowers_static_map_set_call_without_shell` 回归。
39. 新增 monomorphized `List<i64>` direct-call ABI：入口调用点按实参生成 `@lk_fn_X_i64_list`，子函数使用 i64 list storage 支持多 list 参数、`Len`、`GetIndex`、`ListPush`、numeric compare 和 list return；`examples/general/sort_search.lk` 已 true native executable 通过。
40. 新增 `core/src/llvm/subfunction/list.rs`，把 i64-list 子函数编译器从 `subfunction.rs` 拆出；新增 `llvm_backend_lowers_i64_list_direct_call_without_shell` 回归。
41. `List<i64>` 方法参数包 `ArgList` 已在 `Move` 中保持静态形态，静态 range/list receiver 的 `chain/concat` 可物化为 `@lk_concat_i64_list`；新增 `llvm_backend_lowers_i64_range_chain_arglist_without_shell` 回归，`examples/syntax/ranges.lk` true native executable 输出 `ranges: all assertions passed`。
42. `NewList` facts 在含 `List`/`DynamicList` 的参数包中可回查缺失的静态 i64/heap const 参数，覆盖 `iter.flatten([nested_left, nested_right, 4])` 这类混合 `ArgList`；新增 `llvm_backend_lowers_iter_flatten_mixed_arglist_without_shell` 回归，`examples/stdlib/list_iter_sugar.lk` true native executable 已通过。
43. `io.stderr_write/stderr_writeln/stderr_flush` 已映射为 native builtin，stderr 输出走 fd=2 的 `write` lowering，不再误用 stdout `printf`；新增 `core/src/llvm/output/io.rs` 和 `llvm_backend_lowers_io_stderr_to_fd_two_without_shell` 回归，`examples/stdlib/io_demo.lk` native/VM diff 已清零。
44. 静态 module return display 已 native-lower：`return iter;` 在 straightline main 与 block return 中都会打印与 VM 一致的模块 export map，不再静默吞掉 `NativeStraightlineValue::Module`；新增 `core/src/llvm/tests/modules.rs` 和 2 个回归，`/private/tmp/lk-return-iter.lk` 的 VM 与 native executable 输出一致。
45. `math`/`os` module display 已对齐 VM runtime export 显示：`math` 包含 `epsilon/inf/nan/max_*` 等 value exports，`os` callable 名称使用 VM 的 `os::<native>`，`os.env` 子 namespace 可 native 显示；新增 3 个 module display 回归，probe 中 `return math;`、`return os;`、`return os.hostname;`、`return os.env;` 均已 VM/native 输出一致。
46. 非 const 静态 list 元素可走 display-list lowering：`List<object>` return、`List<callable>` return、module/callable/object 混合 display list 都不再触发泛化 unsupported；新增 `llvm_backend_lowers_static_object_list_return_display_without_artifact_shell`、`llvm_backend_lowers_static_callable_list_return_display_without_artifact_shell` 和 `llvm_backend_lowers_static_object_arg_list_len_without_artifact_shell`，probe 中 `list_objects`、`closure_list`、`module_list`、`mixed_display_list`、`object_list_len`、`object_list_index` 均已 VM/native 输出一致。
47. `ArgList` 的 `.len()` block lowering 已覆盖 callable/object display-list；新增 `llvm_backend_lowers_static_callable_arg_list_len_without_artifact_shell` 回归，probe 中 `let xs = [|x| x + 1]; return xs.len();` 的 VM 与 native executable 均输出 `1`。
48. `ArgList` 的 `in`/`Contains` 静态 lowering 已覆盖 object/callable identity；`native_static_contains` 移入 `straightline_value/equality.rs`，并新增 `llvm_backend_lowers_static_callable_arg_list_contains_without_artifact_shell`、`llvm_backend_lowers_static_object_arg_list_contains_without_artifact_shell` 回归，probe 中 `f in [f]`、`user in [user]` 均已 VM/native 输出 `true`。
49. `ArgList` receiver 的 `first`/`last` 静态方法已 native-lower，并修复 block return 对静态 `Object/String/List/Map` 继续按 I64 打印的误编译；新增 `llvm_backend_lowers_static_object_arg_list_first_without_artifact_shell`、`llvm_backend_lowers_static_callable_arg_list_first_without_artifact_shell` 回归，probe 中 `[user].first()`、`[user].last()`、`[f].first()` 均已 VM/native 输出一致。
50. `ArgList` receiver 的 `get(index)` 静态方法已 native-lower；新增 `core/src/llvm/output/arg_list_methods.rs`，把 `ArgList` 方法折叠从接近上限的 `output.rs` 拆出，并新增 `llvm_backend_lowers_static_object_arg_list_get_without_artifact_shell` 回归，probe 中 `[user].get(0)` 已 VM/native 输出 `<User {name: a}>`。
51. `ArgList` receiver 的 `concat/chain/take/skip` 静态方法已 native-lower；`store_native_scalar_call_result` 允许保存静态 `ArgList`，新增 `llvm_backend_lowers_static_object_arg_list_concat_without_artifact_shell`、`llvm_backend_lowers_static_object_arg_list_take_skip_without_artifact_shell` 回归，probe 中 `[a].concat([b])`、`[a].take(1)`、`[a].skip(1)` 均已 VM/native 输出一致。
52. 静态 `List<str>` 的 `get/take/skip/concat/chain` 已 native-lower；新增 `core/src/llvm/output/list_methods.rs`，把静态 list method folding 从 `output.rs` 拆出，并新增 `llvm_backend_lowers_static_string_list_get_without_artifact_shell`、`llvm_backend_lowers_static_string_list_take_skip_concat_without_artifact_shell` 回归。probe 中 `["a", "b"].get(1)`、`take(1)`、`skip(1)`、`concat(["c"])`、`take/skip` 后链式 `concat` 均已 VM/native executable 输出一致。
53. 静态 list method 的 `unique/zip/flatten/chunk` 已 native-lower，并修复 `static_int_list_*` helper 返回 `List` 时 `value` 为空导致直接 return 打印空行的问题；新增 `llvm_backend_lowers_static_list_unique_zip_flatten_chunk_without_artifact_shell` 回归。probe 中 `[1, 2, 1].unique()`、`["a", "b", "a"].unique()`、`[1, 2].zip(["a", "b"])`、`[[1, 2], [3]].flatten()`、`[1, 2, 3].chunk(2)` 和组合返回均已 VM/native executable 输出一致。
54. `List<str>.map(|x| x.upper())` 已 native-lower：facts 不再把静态 list method map 结果记录成无 kind 的静态值，block lowering 可从 `LoadHeapConst List` 动态 list 来源回查 const 元素并执行闭包静态求值；同类 `ArgList([])` 字符串方法参数也已接入静态 string method folding。probe 中 `return ["a", "b"].map(|x| x.upper());` 的 VM 与 native executable 均输出 `[A, B]`。
55. `list` stdlib module 已进入 native-known module 集合，`list.get`/`list.len` 会 canonical 到现有静态 list index/len lowering，而不是走 runtime global shell；新增 `llvm_backend_lowers_list_module_get_without_artifact_shell` 回归。probe 中 `import list; return list.get(["a", "b"], 1);` 的 VM 与 native executable 均输出 `b`。
56. `list.first`/`list.last`/`list.contains`/`list.is_empty`/`list.join`/`list.concat` 已 canonical 到现有静态 list primitive，不再为这些静态 stdlib helper 走 runtime global shell；新增 `llvm_backend_lowers_static_list_module_helpers_without_artifact_shell` 回归。probe 中 `import list; return [list.first(["a","b"]), list.last(["a","b"]), list.contains(["a","b"], "b"), list.is_empty([]), list.join(["a","b"], "-"), list.concat(["a"], ["b"])]` 的 VM 与 native executable 均输出 `[a, b, true, true, a-b, [a, b]]`。
57. `list.push`/`list.reverse`/`list.pop`/`list.index_of`/`list.slice`/`list.sort`/`list.set`/`list.insert`/`list.remove_at` 的静态 module helper 已 native-lower，继续复用静态 list primitive 而不是 runtime shell；新增 `llvm_backend_lowers_static_list_module_more_helpers_without_artifact_shell`、`llvm_backend_lowers_static_list_module_mutators_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/list_module_more.lk` 和 `/private/tmp/lk-shape-probes/list_module_mutators.lk` 均已 VM/native executable 输出一致。
58. `string.upper`/`string.lower`/`string.trim`/`string.starts_with`/`string.ends_with`/`string.contains`/`string.replace`/`string.substring`/`string.reverse`/`string.repeat`/`string.char`/`string.byte`/`string.chars`/`string.find`/`string.is_empty` 的静态 module helper 已 native-lower；新增 `core/src/llvm/output/string_methods.rs` 和统一 `StringModuleMethod` builtin，避免为每个 string helper 增加独立 enum variant；新增 `llvm_backend_lowers_static_string_module_basic_helpers_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/string_module_basic.lk` 已 VM/native executable 输出一致。
59. `string.split`/`string.join`/`string.strip`/`string.strip_prefix`/`string.strip_suffix`/`string.count`/`string.pad_left`/`string.pad_right`/`string.to_int`/`string.to_float`/`string.title`/`string.capitalize`/`string.format` 的静态 module helper 已 native-lower；新增 `llvm_backend_lowers_static_string_module_more_helpers_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/string_module_more.lk` 已 VM/native executable 输出一致。
60. `map.len`/`map.keys`/`map.values`/`map.has`/`map.get` 的静态 module helper 已 native-lower；新增 `core/src/llvm/output/map_methods.rs` 和统一 `MapModuleMethod` builtin，继续复用静态 map len/index/contains primitive，不走 runtime global shell；新增 `llvm_backend_lowers_static_map_module_basic_helpers_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/map_module_basic.lk` 的 VM 与 native executable 均输出 `[2, [a, b], [1, 2], true, 1, nil]`。
61. `iter.next`/`iter.collect` 的静态 module helper 已 native-lower；新增 `core/src/llvm/output/iter_methods.rs` 和统一 `IterModuleMethod` builtin，`next` canonical 到静态 index 0，`collect` 保持静态 list value，不走 runtime global shell；新增 `llvm_backend_lowers_static_iter_next_collect_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/iter_next_collect.lk` 的 VM 与 native executable 均输出 `[1, nil, [a, b], [0, 1, 2]]`。
62. `iter.enumerate`/`iter.take`/`iter.skip`/`iter.chain`/`iter.flatten`/`iter.unique`/`iter.chunk`/`iter.zip` 的静态 module helper 已 native-lower；这些 `Iter*` builtin 直接复用 `output/list_methods.rs` 的静态 list method folding，不再落到泛化 unsupported；新增 `llvm_backend_lowers_static_iter_list_helpers_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/iter_module_static_more.lk` 的 VM 与 native executable 均输出 `[[[0, a], [1, b]], [1, 2], [2, 3], [1, 2, 3], [1, 2, 3], [1, 2], [[1, 2], [3]], [[1, a], [2, b]]]`。
63. `iter.map`/`iter.filter`/`iter.reduce` 的静态 module helper 已接入 straightline canonical lowering，复用现有静态 list + closure evaluator，不再为了这些 stdlib helper 引入 runtime global shell；新增 `llvm_backend_lowers_static_iter_map_filter_reduce_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/iter_map_filter_reduce.lk` 的 VM 与 native executable 均输出 `[[2, 3, 4], [2, 3], 6]`。同时 native stderr 写入前会 flush libc stdout，`io.stdout_flush()` 发出 `fflush(NULL)`，恢复 `io_demo.lk` 的 VM/native 合并流顺序一致性。
64. `math.inf/nan/max_int/min_int/max_float/epsilon` 和 `math.tan/asin/acos/atan/atan2/log/log10/log2/clamp/hypot/cbrt/sinh/cosh/tanh/trunc/fract/sign/to_int/to_float/is_nan/is_inf` 的静态 module helper 已 native-lower；新增 `core/src/llvm/output/math_methods.rs` 和统一 `MathModuleMethod` builtin，继续按 compile-time native primitive/常量折叠，不引入 stdlib runtime shell。probe 中 `/private/tmp/lk-shape-probes/math_more.lk` 的 VM 与 native executable 均输出 `[0, 0, 3, true, true]`，新增 `llvm_backend_lowers_static_math_module_more_helpers_without_artifact_shell` 回归。
65. 修复 `map.set({}, "a", "x")` 后 `map.get(m, "a")` / `m["a"]` 的 ShortStr/String key 不一致：`stdlib/src/map.rs` 的 Mixed-map `set/delete` 现在按 `RuntimeMapKey::ShortStr` canonicalize 短字符串，并按 `RuntimeMapKey::as_str()` 匹配等价字符串 key；`map.has/get` 使用 `TypedMap::get_str`，避免 native stdlib 与 VM `GetIndex` 语义分叉。probe 中 `/private/tmp/lk-shape-probes/map_dynamic_string_values.lk` 的 VM 与 native executable 均输出 `[true, x, x, [x], [{}, x]]`；新增 `llvm_backend_lowers_string_value_map_set_get_without_shell` 回归。
66. `list` stdlib module 的 `reverse/pop/push/slice/insert/remove_at/set/index_of` 已覆盖 `List<object>` / display-list `ArgList` lowering；这些 shape 现在在 `core/src/llvm/output/list_methods.rs` 中直接生成静态 `ArgList` 结果，不走 VM shell，并且 `list.index_of` / `list.contains` 对 `ArgList` 使用 stdlib `RuntimeVal` 子集相等语义，避免把对象结构相等误编译为命中。probe 中 `/private/tmp/lk-shape-probes/list_module_object_mutators.lk` 的 VM 与 native executable 均输出 `[[<User {name: b}>, <User {name: a}>], <User {name: b}>, [<User {name: a}>, <User {name: b}>], [<User {name: a}>], [<User {name: b}>, <User {name: a}>], [[<User {name: b}>], <User {name: a}>], [[<User {name: a}>, <User {name: a}>], <User {name: b}>], -1]`；新增 `llvm_backend_lowers_static_object_list_module_mutators_without_artifact_shell` 回归。
67. 静态 `Map<str, object>` / `Map<str, callable>` 已支持 display-map lowering：`NativeStraightlineValue::DisplayMap` 可保存非 const native value，`map.set/get/has/values/delete`、直接索引、`len`、`is map`、display return 和静态 contains 都不再要求 value 可转成 `ConstRuntimeValue32Data`。新增 `core/src/llvm/straightline_value/maps.rs`，把 map helper 从接近上限的 `straightline_value.rs` 拆出；新增 `llvm_backend_lowers_static_object_map_module_values_without_artifact_shell`、`llvm_backend_lowers_static_callable_map_module_values_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/map_object_values.lk` 的 VM/native executable 均输出 `[true, <User {name: a}>, [<User {name: a}>, <User {name: b}>], [{b: <User {name: b}>}, <User {name: a}>]]`，`/private/tmp/lk-shape-probes/map_callable_values.lk` 的 VM/native executable 均输出 `[true, <fn #1(0 captures)>, [<fn #1(0 captures)>], [{}, <fn #1(0 captures)>]]`。
68. DisplayMap 邻接 shape 已补充回归：`map.len/keys`、直接索引、`in`、direct return、`if let {"a": first, ..rest}`、`map.values(users).len/first/last/get`、以及 `Map<str, object>` equality 都可 native-lower，不走 artifact shell；新增 `llvm_backend_lowers_static_display_map_more_operations_without_artifact_shell`、`llvm_backend_lowers_static_display_map_rest_without_artifact_shell`、`llvm_backend_lowers_static_display_map_values_methods_without_artifact_shell`、`llvm_backend_lowers_static_display_map_equality_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/map_display_shape_more.lk`、`map_display_rest.lk`、`map_display_values_methods.lk`、`map_display_equality.lk` 均已 VM/native executable 输出一致。
69. `Map<str, object>` 的 display-map iteration 已 native-lower：`map.set({}, "a", object)` 在静态 key/value 场景会优先 canonicalize 回 `DisplayMap`，`ArgList` 动态索引不会把循环变量误折叠成初始常量，control-flow boundary 会保留 `DisplayMap`/`ArgList` 静态容器；新增 `llvm_backend_lowers_static_display_map_iter_without_artifact_shell` 回归。probe 中 `/private/tmp/lk-shape-probes/map_display_iter.lk` 的 VM 与 native executable 均输出 `[[a, <User {name: a}>], [b, <User {name: b}>]]`。
70. 修复 `examples/syntax/for_loop_patterns.lk` native：tuple destructuring mixed pair list 和 string iteration 都会把动态字符串元素物化到 ptr-list storage，`List<str>`/`List<ptr>` 与静态字符串列表比较改为 length-gated `strcmp`，避免读未初始化 ptr slot，也不再用 `text.len` 代替真实内容比较。新增 `llvm_backend_lowers_for_destructured_string_list_compare_without_shell`、`llvm_backend_lowers_string_iteration_char_list_compare_without_shell` 回归；`/private/tmp/for_pairs_assert.lk` native 输出 `ok pairs`，`examples/syntax/for_loop_patterns.lk` true native executable 输出 `for_loop_patterns: all assertions passed`。
71. 修复 `examples/general/config_parser.lk` native：`toml.parse(...)` 的静态结果在 `cfg.ssl.has(feature)` / `cfg.ssl[feature]` 这类 method-call + dynamic key control-flow shape 中可继续恢复。`Call` 分支的 recovered/scalar/runtime builtin fallback 已从 `blocks.rs` 抽入 `core/src/llvm/scalar/blocks/call_args.rs`，`GetIndex` 新增支配当前 pc 的静态字符串 key recovery，不放宽全局 `static_register_value_trusted_before`。新增 `llvm_backend_lowers_static_parse_result_method_call_with_recovered_key_without_shell` 回归；`examples/general/config_parser.lk` true native executable 输出 `config_parser: all assertions passed`。
72. 修复嵌套静态 `List<i64>` 动态迭代 native 误编译：`matrix[i]` 经 `ToIter` 跨 basic block 后仍能恢复为 `DynamicConstListElement`，内层 `row[j]` 不再把循环变量初始化值 `0` 当静态 index，而是生成 outer+inner dynamic select。新增 `llvm_backend_lowers_nested_static_i64_list_iteration_without_artifact_shell` 回归；最小 probe `/private/tmp/lk-nested-int-list.lk` 的 VM/native 均输出 `ok nested int`，`examples/syntax/control_flow.lk` true native executable 输出 `control_flow: all assertions passed`。
73. 修复控制流后字符串格式化打印的 stale static fact：`println("g = {}", g)` 这类多参数 formatted print 在检测到参数来自静态字符串回查但寄存器 kind 是 `StrPtr` 时，改为按真实寄存器 slot 打印，避免把 `else` 分支旧静态值烘进 `@lk_fmt_arg_*`。新增 `llvm_backend_formats_control_flow_assigned_string_from_slot_without_artifact_shell` 回归；`examples/syntax/unsupported.lk` 已 VM/native diff 清零。同时 formatted print helper 只接管需要 runtime string slot 的参数，避免 `examples/syntax/trait_impl.lk` 的 `Float` trait method result 被按 i64 打印；`trait_impl.lk` 已 VM/native diff 清零。
74. 修复 `DynamicMap<str,f64>` 动态迭代 value lowering：`ToIter`/`DynamicMapEntry` facts 现在保留 map value kind，`pair[1]` 对 f64 map 会读取 `%mapN.f64.slots` 并写入 double register，不再只支持 `Map<str,i64>` entry value。新增 `llvm_backend_lowers_dynamic_string_f64_map_iteration_without_artifact_shell` 回归；probe `/private/tmp/lk-shape-probes/dynamic_loop_map_str_f64_iter_sum.lk` 的 VM/native executable 均输出 `4`。
75. 新增动态二维 pair-list native representation：`out.push([pair[0], pair[1]])` 这类 `Map<str,f64>` 迭代结果会 lowering 为 `DynamicPairList { first: StrPtr, second: F64 }`，按列使用 ptr/f64 slots 保存真实值，return 时打印 `[[k1, 1.5], [k2, 2.5]]`，不再退化为普通 `DynamicList<i64>`。动态 map iterator key 现在会把 `(prefix, number)` 用 `snprintf("%s%ld")` 重建完整 key，避免只显示 `k`。新增 `llvm_backend_lowers_dynamic_string_f64_map_pair_list_return_without_artifact_shell` 回归；probe `/private/tmp/lk-shape-probes/dynamic_loop_map_str_f64_iter.lk` 的 VM/native executable 均输出 `[[k1, 1.5], [k2, 2.5]]`。
76. 新增 `DynamicMap<str,i64>` / `DynamicMap<str,f64>` 本体 return display lowering：block return 会直接遍历 map prefix/number/value slots，按 `{k1: 10, k2: 20}` 和 `{k1: 1.5, k2: 2.5}` 打印，不再因为动态 map 本体 return 触发泛化 unsupported。新增 `llvm_backend_lowers_dynamic_string_i64_map_return_without_artifact_shell`、`llvm_backend_lowers_dynamic_string_f64_map_return_without_artifact_shell` 回归；probe `/private/tmp/lk-shape-probes/dynamic_loop_map_str_i64_return.lk` 和 `/private/tmp/lk-shape-probes/dynamic_loop_map_str_f64_return.lk` 的 VM/native executable 输出一致。
77. 新增 `map.keys(DynamicMap<str,T>)` native lowering：动态 string map keys 会复制到 monomorphized `DynamicList<StrPtr>`，带数字后缀的 key 使用 `snprintf("%s%ld") + strdup` 重建完整字符串，避免 `"k${n}"` 退化成前缀 `k`。同时修复 ArgList return 中连续动态列表的 PHI predecessor 生成，`return [map.keys(weights), map.values(weights)]` 这类组合可 true native 编译并输出 `[[k1, k2], [1.5, 2.5]]`。新增 `llvm_backend_lowers_dynamic_string_f64_map_keys_values_without_artifact_shell` 回归；probe `/private/tmp/lk-shape-probes/dynamic_loop_map_str_f64_keys_values.lk` 的 VM/native executable 输出一致。
78. 修复 `DynamicList<f64>` 的 method-call native 误编译：`xs.take(1)` / `xs.skip(1)` / `xs.concat([3.5])` 现在会保持 `DynamicList<F64>` 并调用 monomorphized double-list slice/take/concat helper，不再在 ArgList return 中被当作 scalar `0` 打印。新增 `core/src/llvm/dynamic_containers/f64_lists.rs`，避免 `dynamic_containers.rs` 超过 1500 行；新增 `llvm_backend_lowers_dynamic_f64_list_methods_without_artifact_shell` 回归。probe `/private/tmp/lk-shape-probes/dynamic_loop_f64_list_methods.lk` 的 VM/native executable 均输出 `[[1.5], [2.5], [1.5, 2.5, 3.5]]`。
79. 修复 `DynamicList<f64>` 的 `list` module helpers native 误编译：`list.contains(xs, 1.5)` / `list.index_of(xs, 2.5)` 会调用 double-list contains/index helper，`list.sort(xs)` / `list.reverse(xs)` 会生成新的 `DynamicList<F64>`，`list.pop(xs)` 会读取最后一个 double，不再被 unsupported builtin fallback 空跳成旧寄存器值 `0`。新增 `llvm_backend_lowers_dynamic_f64_list_module_methods_without_artifact_shell` 回归；probe `/private/tmp/lk-shape-probes/dynamic_loop_f64_list_module_methods.lk` 的 VM/native executable 均输出 `[true, 1, [1.5, 1.5, 2.5], [1.5, 2.5, 1.5], 1.5]`。
80. 修复静态 `ArgList` display 中整数值 f64 的 VM/native 差异：`string.to_float(2)` 在静态组合返回中会按 VM display 打印为 `2`，不再输出 `2.0`；`llvm_backend_lowers_static_string_module_more_helpers_without_artifact_shell` 已覆盖该路径。
81. 修复 shape probe sweep 暴露的 3 个 VM_OK native 缺口：静态 `list.join(["a", "b"], "-")` 不再生成未定义的 `@lk_list_join_*` 符号，而是走已有本地字符串 materialization；`math.epsilon` 这类静态 f64 member return / formatted print 使用 VM display 字符串，不再被 libc `%.16g` 打成科学计数；`os.env.get` / `set` / `unset` 现在可作为 `OsEnv` module member native callable display。新增 `llvm_backend_lowers_math_epsilon_member_display_without_artifact_shell`、`llvm_backend_lowers_os_env_get_member_display_without_artifact_shell` 回归，并增强 `llvm_backend_lowers_static_list_module_helpers_without_artifact_shell` 断言。全量 `/private/tmp/lk-shape-probes/*.lk` sweep 已恢复为 `TOTAL=152 VM_OK=136 VM_FAIL=16 COMPILE_FAIL=0 NATIVE_FAIL=0 DIFF=0`（`SWEEP_DIR=/private/tmp/lk-probe-sweep-2owweh`）。
82. 修复 `DynamicList<str>` 的 `list` module helpers native 误编译：`list.contains(xs, "v2")` / `list.index_of(xs, "v3")` 会调用 ptr-list `strcmp` helper，`list.reverse(xs)` / `list.sort(xs)` 会生成新的 `DynamicList<StrPtr>`，`list.pop(xs)` 会返回并按 `StringPtr` 打印，不再落到 unsupported builtin fallback 或把 ptr 当 i64 输出。新增 `core/src/llvm/dynamic_containers/ptr_lists.rs`，`call_args.rs` 的 recovered builtin call 路径也已接入 ptr-list lowering；facts 层按 monomorphized `DynamicList<StrPtr|Text>` 推导 module helper return shape。新增 `llvm_backend_lowers_dynamic_string_list_module_methods_without_artifact_shell` 回归。targeted probes 中 `dynamic_loop_string_list_module_methods_extra.lk` VM/native 均输出 `[true, 2, [v3, v2, v1], [v1, v2, v3], v3]`；全量 `/private/tmp/lk-shape-probes/*.lk` sweep 为 `TOTAL=156 VM_OK=140 VM_FAIL=16 COMPILE_FAIL=0 NATIVE_FAIL=0 DIFF=0`。
83. 修复 `DynamicList<str>` / `DynamicList<f64>` 的 `list` module mutators native lowering：`list.push`、`list.slice`、`list.insert`、`list.remove_at`、`list.set` 会按 monomorphized `DynamicList<StrPtr>` / `DynamicList<F64>` 生成新 list 或嵌套返回 `[[new_list], old]`，不再被静态 heap-const replacement 抢先折叠，也不退回 runtime shell。新增 ptr-list/f64-list helper wrapper，`returns.rs` 支持 nested dynamic `ArgList` display，facts 层扩展 module mutator propagation。新增 `llvm_backend_lowers_dynamic_string_list_module_mutators_without_artifact_shell`、`llvm_backend_lowers_dynamic_f64_list_module_mutators_without_artifact_shell` 回归。targeted probes 中 `/private/tmp/lk-shape-probes/dynamic_loop_string_list_module_mutators_extra.lk` VM/native 均输出 `[[v1, v2, v3, v4], [v2, v3], [v1, vx, v2, v3], [[v1, v3], v2], [[v1, vy, v3], v2]]`；`/private/tmp/lk-shape-probes/dynamic_loop_f64_list_module_mutators_extra.lk` VM/native 均输出 `[[1.5, 2.5, 3.5, 4.5], [2.5, 3.5], [1.5, 9.5, 2.5, 3.5], [[1.5, 3.5], 2.5], [[1.5, 8.5, 3.5], 2.5]]`。全量 `/private/tmp/lk-shape-probes/*.lk` sweep 为 `TOTAL=158 VM_OK=142 VM_FAIL=16 COMPILE_FAIL=0 NATIVE_FAIL=0 DIFF=0`（`SWEEP_DIR=/private/tmp/lk-probe-sweep-bJvKln`）。
84. 修复 VM compiler 对 receiver method mutator 参数的 move 语义：`xs.push(local)` 和 `xs.set(i, local)` 现在只对非当前 local 的临时值设置 `move_value=true`，不会把后续仍要读取的 local 参数 `take` 成 `nil`。这修复了 `/private/tmp/lk-shape-probes/list_display_methods_more.lk` 中 `[a].push(b)` 消耗 `b`，导致后续 `[a].insert(0, b)` 和 `[a, b].remove_at(0)` VM 输出 `nil` 的问题；VM 与 native executable 现在均输出 `[[<User {name: b}>, <User {name: a}>], <User {name: b}>, [<User {name: a}>, <User {name: b}>], [<User {name: a}>], [<User {name: b}>, <User {name: a}>], [[<User {name: b}>], <User {name: a}>], nil]`。新增 `compiler32_push_method_does_not_consume_local_argument`、`compiler32_set_method_does_not_consume_local_value_argument` 回归。全量 `/private/tmp/lk-shape-probes/*.lk` sweep 更新为 `TOTAL=158 VM_OK=151 VM_FAIL=7 COMPILE_FAIL=0 NATIVE_FAIL=0 DIFF=0`（`SWEEP_DIR=/private/tmp/lk-probe-sweep-uxRCR2`）。

### scalar_facts.rs 文件大小

当前 LLVM 相关主要文件均低于或等于 1500 行；本轮拆出 `core/src/llvm/scalar/facts/map_methods.rs`、`core/src/llvm/scalar/facts/entry.rs`、`core/src/llvm/scalar/facts/arg_lists.rs`、`core/src/llvm/scalar/facts/list_push.rs`、`core/src/llvm/scalar/blocks/len.rs`、`core/src/llvm/scalar/blocks/map_methods.rs`、`core/src/llvm/scalar/blocks/list_methods.rs`、`core/src/llvm/scalar/blocks/list_push.rs`、`core/src/llvm/scalar/blocks/not.rs`、`core/src/llvm/scalar/blocks/iter.rs`、`core/src/llvm/scalar/blocks/string_methods.rs`、`core/src/llvm/subfunction/list.rs`、`core/src/llvm/output/io.rs`、`core/src/llvm/output/arg_list_methods.rs`、`core/src/llvm/output/list_methods.rs`、`core/src/llvm/output/string_methods.rs`、`core/src/llvm/output/map_methods.rs`、`core/src/llvm/output/iter_methods.rs`、`core/src/llvm/output/math_methods.rs`、`core/src/llvm/tests/modules.rs`、`core/src/llvm/straightline_value/maps.rs`。当前行数快照：`blocks.rs` 1413、`facts.rs` 1435、`block_helpers.rs` 1499、`output.rs` 1449、`output/io.rs` 137、`output/arg_list_methods.rs` 75、`output/list_methods.rs` 620、`output/string_methods.rs` 438、`output/map_methods.rs` 139、`output/iter_methods.rs` 76、`output/math_methods.rs` 185、`scalar/contains/int_lists.rs` 926、`scalar/facts/analysis.rs` 642、`scalar/blocks/returns.rs` 100、`straightline_main.rs` 1143、`straightline_value.rs` 1494、`straightline_value/maps.rs` 111、`straightline_value/equality.rs` 259、`straightline_value/display.rs` 451、`straightline_value/modules.rs` 273、`tests.rs` 1485、`tests/modules.rs` 421、`tests/objects.rs` 657、`tests/strings.rs` 871、`stdlib/src/map.rs` 859。

### 当前剩余 LLVM 缺口

- 2026-06-02：动态数值容器继续推进。已修复 `DynamicList<i64>` return 只打印首元素的误编译，新增 `DynamicList<f64>` push/get/return lowering，新增 `DynamicMap<str,f64>` set/get lowering，`"k${n}"` 这类动态文本 key 会 canonicalize 为 `(static prefix, dynamic number)`，避免保存可变 text buffer 指针导致前一轮 key 被覆盖。`map.values(DynamicMap<str,f64>)` 会 copy 到 monomorphized `DynamicList<f64>`，嵌套返回 `[map.get(...), map.values(...)]` 可 native-lower。动态字符串 list return 已按 `DynamicList<StrPtr>` 保持 element kind，并在 `push` 时复制字符串指针，避免循环复用同一个 text buffer 导致 `[v2, v2]`。新增回归：`llvm_backend_lowers_dynamic_i64_list_return_without_artifact_shell`、`llvm_backend_lowers_dynamic_f64_list_return_without_artifact_shell`、`llvm_backend_lowers_dynamic_string_f64_map_get_without_artifact_shell`、`llvm_backend_lowers_dynamic_string_f64_map_values_without_artifact_shell`、`llvm_backend_lowers_dynamic_string_list_return_without_artifact_shell`。
- 2026-06-02 probe 结果：`dynamic_loop_i64_list_return.lk` VM/native 均输出 `[1, 2]`；`dynamic_loop_f64_list_return.lk` VM/native 均输出 `[1.5, 2.5]`；`dynamic_loop_bool_list_return.lk` VM/native 均输出 `[false, true, true]`; `dynamic_loop_string_list_return.lk` VM/native 均输出 `[v1, v2]`；`dynamic_loop_map_str_f64_sum.lk` VM/native 均输出 `4`；`dynamic_loop_map_str_f64.lk` VM/native 均输出 `[1.5, 2.5, [1.5, 2.5]]`。
- 2026-06-02 当前验证状态：动态过滤 `cargo test -p lk-core --features llvm llvm_backend_lowers_dynamic_ -- --nocapture` 通过，13 passed。完整 `cargo test -p lk-core --features llvm llvm::tests:: -- --nocapture` 已恢复通过，219 passed / 0 failed。本轮还修复了 `List<i64>` inline `take(i).concat([item]).concat(skip(i))`：动态 list method 现在优先于静态 heap-const 回退，direct-call list return facts 会按实际实参推断 `DynamicList<i64>` / `DynamicList<ptr>`，不再固定成 `StrPtr`。`DivFloat` 除零场景已改为支持带 `lk_divisor_zero` guard 的 native lowering，旧 unsupported 测试已更新为 guard 断言。
- 2026-06-02 文件大小快照：`output.rs` 1465、`block_helpers.rs` 1500、`facts.rs` 1492、`facts/analysis.rs` 653、`blocks.rs` 1412、`straightline_value.rs` 1495、`dynamic_containers.rs` 1368、`dynamic_containers/f64_lists.rs` 398、`scalar/blocks/returns.rs` 600、`scalar/blocks/list_methods.rs` 828、`scalar/facts/map_methods.rs` 547、`output/return_value.rs` 110、`output/list_methods.rs` 619、`straightline_value/display.rs` 463、`tests/modules.rs` 481、`tests/basic.rs` 1002、`tests/strings.rs` 951，仍低于或等于 1500 行限制。
- `examples/general/word_count.lk`、`examples/stdlib/map_demo.lk`、`examples/general/sort_search.lk` 已通过 true native executable 验证；但这些只是 workload 证明，不等于 LLVM shape 全覆盖完成。
- `examples/syntax/ranges.lk`、`examples/general/higher_order.lk`、`examples/stdlib/list_ops.lk`、`examples/syntax/closure.lk`、`examples/syntax/struct_trait.lk`、`examples/stdlib/list_iter_sugar.lk` 已通过 true native executable 验证。
- 当前 examples sweep 剩余 LLVM 直接缺口：2026-06-02 最新完整 sweep 为 `TOTAL=47 VM_OK=40 VM_FAIL=7 COMPILE_FAIL=0 NATIVE_FAIL=0 NATIVE_OK=40 DIFF=0`（`SWEEP_DIR=/private/tmp/lk-examples-sweep-1780385424`）。剩余 7 个 VM_FAIL 需要按 VM/typechecker/import resolver 问题单独处理。
- 本轮 shape probe 已确认 `List<callable>.len()`、`List<object>.len()`、`List<object>[0]`、`f in [f]`、`user in [user]`、`[user].first()`、`[user].last()`、`[f].first()`、`[user].get(0)`、`[a].concat([b])`、`[a].take(1)`、`[a].skip(1)`、`List<object>` return、`List<callable>` return、`Map<str, object>` 的 `set/get/has/values/delete/keys/len/direct return/rest/equality/iteration`、`Map<str, callable>` 的 `set/get/has/values/delete/keys/direct return`、`map.values(Map<str, object>).len/first/last/get`、`import list; list.reverse/pop/push/slice/insert/remove_at/set/index_of(List<object>, ...)`、`List<str>.get(1)`、`List<str>.take(1)`、`List<str>.skip(1)`、`List<str>.concat(["c"])`、`List<str>.map(|x| x.upper())`、`import list; list.get(["a", "b"], 1)`、`import list; list.first/last/contains/is_empty/join/concat(...)`、`import list; list.push/reverse/pop/index_of/slice/sort/set/insert/remove_at(...)`、`import string; string.upper/lower/trim/starts_with/ends_with/contains/replace/substring/reverse/repeat/char/byte/chars/find/is_empty(...)`、`import string; string.split/join/strip/strip_prefix/strip_suffix/count/pad_left/pad_right/to_int/to_float/title/capitalize/format(...)`、`import map; map.len/keys/values/has/get(...)`、`import iter; iter.next/collect/enumerate/take/skip/chain/flatten/unique/chunk/zip/map/filter/reduce(...)`、`import math; math.tan/log/to_int/is_nan/is_inf(...)` 以及其它静态 math pure helpers、`List<str>` 链式 `take/skip/concat`、`List<i64>.unique()`、`List<str>.unique()`、`List<i64>.zip(List<str>)`、嵌套 i64 list `flatten()`、i64 list `chunk(2)` 均可 native-lower。下一步应继续把 callable/object/container display shape 的剩余 stdlib helper 纳入矩阵，并继续把 stdlib module 函数从 runtime global shell 迁到 canonical native lowering。
- `DynamicList<Text>`/`DynamicList<StrPtr>` 已具备 `sort_words` 所需真实 ptr-list representation；`DynamicList<I64>` 已具备 `merge(List<i64>, List<i64>) -> List<i64>` 所需 direct-call/subfunction representation；后续仍需把这些路径纳入更系统的 shape matrix。
- monomorphized list ABI 当前覆盖 `List<str>`/`List<ptr>` 和 `List<i64>` 的部分方法/子函数形态；还未扩展为完整 `List<T>`/`Map<K,V>` 矩阵。
- 2026-06-02 最新验证：`cargo fmt --all` 通过；`cargo check -p lk-core --features llvm` 通过；`cargo build -p lk-cli --features llvm` 通过；`cargo test -p lk-core compiler32_push_method_does_not_consume_local_argument -- --nocapture` 通过；`cargo test -p lk-core compiler32_set_method_does_not_consume_local_value_argument -- --nocapture` 通过；`cargo test -p lk-core --features llvm llvm_backend_lowers_dynamic_ -- --nocapture` 通过，16 passed / 0 failed；`cargo test -p lk-core --features llvm llvm::tests:: -- --nocapture` 通过，224 passed / 0 failed；`cargo test -p lk-core -q` 通过，820 passed / 0 failed；`target/debug/lk /private/tmp/lk-shape-probes/list_display_methods_more.lk` 与 native executable 输出一致；全量 `/private/tmp/lk-shape-probes/*.lk` native sweep 为 `TOTAL=158 VM_OK=151 VM_FAIL=7 COMPILE_FAIL=0 NATIVE_FAIL=0 DIFF=0`。当前行数快照：`core/src/vm/compiler32/call.rs` 866、`core/src/vm/compiler32/tests/call_intrinsics.rs` 516、`blocks.rs` 1473、`facts.rs` 1497、`block_helpers.rs` 1500、`straightline_value.rs` 1497，仍低于或等于 1500 行限制。下一步仍应继续把 callable/object/container display shape 的剩余 stdlib helper 纳入矩阵，并继续把 stdlib module 函数从 runtime global shell 迁到 canonical native lowering。

## 下一步建议

1. 建立“语言 shape -> Instr32/runtime shape -> LLVM lowering”覆盖矩阵，不能只用 examples 代替完成标准。
2. 扫描并压缩剩余泛化 unsupported reason，优先让每个失败都指向具体缺失 shape。
3. 继续推进 native stdlib：`stdlib/lk` pure source、meta-method canonical lowering、monomorphized stdlib functions。
4. 决定 callable/object/container 非 scalar return 的 native display/representation 方案；实现前保持明确 unsupported，不允许误编译。
5. 继续拆分 `blocks.rs`、`facts.rs`、`straightline_value.rs` 这类接近 1500 行文件，给后续 LLVM 收口留空间。
